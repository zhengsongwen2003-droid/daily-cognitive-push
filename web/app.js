const cards = [
  {
    title: "第一期：为什么“有能力的刺头”常被提拔，而“听话的老实人”总在原地踏步？",
    theme: "职场政治",
    tags: ["不可替代性", "利益交换", "驭人术"],
    caseText: [
      "在某单位里，有两个员工。",
      "小实工作勤勤恳恳，从不迟到早退，对领导交代的任务绝对服从，性格温和，人缘也不错。大家都觉得他踏实、可靠、不会惹事。",
      "大圣则完全不同。他业务能力极强，能解决别人搞不定的核心问题，但脾气也大，偶尔会和领导公开争论，甚至在某些政策上表达不同意见，是个公认的“刺头”。",
      "年底有一个关键的副科级或主管空缺。很多人以为，按照“听话、顺从、稳定”的原则，领导一定会选小实。但出乎意料的是，领导最终顶住压力，提拔了大圣。",
      "普通人看这个结果，容易陷入“好人没好报”的道德情绪。但如果切入管理政治学视角，领导的选择其实非常理性。"
    ],
    analysis: [
      {
        title: "核心资源的对等：用“不可替代性”置换“特权”",
        body: "在政治和社会博弈中，核心竞争力决定了你的边界。大圣之所以敢当刺头，是因为他手里握着部门的核心 KPI 或不可替代的技术。对领导而言，大圣的“闹”是成本，但他带来的业绩，是领导向上爬或坐稳位置的筹码。提拔大圣，本质上是在向组织释放信号：只要你能创造无可替代的价值，你就可以拥有一定的豁免权。"
      },
      {
        title: "老实人的陷阱：沉默的螺丝钉最难被挪动",
        body: "小实的问题不是不努力，而是太好用了。在管理学里有个残酷逻辑：如果一个员工在当前岗位上效率高、成本低、从不抱怨，那么让他原地不动，往往是组织成本最低的选择。一旦把小实提拔到管理岗，领导不仅失去一个稳定执行者，还要承担他无法驾驭复杂局面的风险。"
      },
      {
        title: "驭人术的考量：收服刺头会产生政治红利",
        body: "从心理和权力手腕看，恩威并施用在刺头身上效果更明显。大圣知道自己平时越界，当领导力排众议提拔他时，他更容易感到“知遇之恩”和心理亏欠，后续反而可能收敛锋芒，为领导解决更难的问题。提拔小实，小实多半觉得这是按部就班应得的，政治回报感没有那么强。"
      }
    ],
    insights: [
      {
        label: "对下",
        text: "服从诚可贵，但不可替代的价值才是硬通货。没有能力的顺从，在利益博弈中很容易被当作低成本资源。"
      },
      {
        label: "对上",
        text: "领导不一定怕你有棱角，真正怕的是你既没有棱角，也不能帮他遮风挡雨。"
      },
      {
        label: "社交人情",
        text: "适度展现“不好惹”和“有底线”，反而能提高你的政治分量。一味做老好人，只会让自己的利益空间被不断挤压。"
      }
    ],
    questionText: "我现在在某个关系或组织里，是“不可替代的人”，还是“好用但随时可替换的人”？",
    actionText: "花 10 分钟列出你当前岗位或关系中的三个筹码：你能解决什么关键问题、谁离不开它、它是否被看见。"
  },
  {
    title: "第二期：为什么会哭会闹的人更容易被照顾？",
    theme: "人情博弈",
    tags: ["情绪成本", "资源分配", "边界"],
    caseText: [
      "同一个团队里，小安总是自己消化压力，哪怕任务不合理，也只是默默加班。小梅则不同，遇到资源不足会直接讲出来，压力太大也会让领导知道。",
      "时间久了，一个微妙现象出现了：领导经常主动问小梅需不需要支持，却很少问小安。小安觉得委屈：明明我更懂事，为什么反而没人照顾？",
      "这不是简单的公平问题，而是组织里很常见的资源分配逻辑。谁的问题更显性，谁制造的潜在成本更高，谁就更容易被管理者优先处理。"
    ],
    analysis: [
      {
        title: "沉默会降低你的问题优先级",
        body: "管理者的注意力有限，通常会优先处理看得见、会扩散、可能失控的问题。你越能忍，别人越容易默认你还撑得住。"
      },
      {
        title: "情绪也是一种成本信号",
        body: "会表达不满的人不一定更有理，但他让对方感受到“不处理会有后果”。在人情场里，适度表达边界，是让别人重新计算成本。"
      },
      {
        title: "懂事的人要学会让需求可见",
        body: "成熟不是永远不麻烦别人，而是清楚、具体、不过度地表达需求。你不说，别人就会把你的承受力当作默认资源。"
      }
    ],
    insights: [
      { label: "对自己", text: "不要把忍耐误认为高级。长期不表达，只会让别人低估你的成本。" },
      { label: "对关系", text: "真正稳定的关系，不靠一个人无限懂事，而靠双方都能看见边界。" },
      { label: "对职场", text: "提出困难时，不要只抱怨，要同时给出影响、风险和需要的支持。" }
    ],
    questionText: "我最近有没有把自己的压力藏得太好，以至于别人默认我不需要支持？",
    actionText: "用 10 分钟写一段具体表达：我现在遇到什么困难，如果不处理会有什么影响，我需要哪一种支持。"
  },
  {
    title: "第三期：为什么功劳不能只等别人看见？",
    theme: "职场可见度",
    tags: ["功劳归属", "汇报", "影响力"],
    caseText: [
      "一个项目能按时交付，小陈做了大量底层工作：整理数据、修补流程、协调细节。但最终汇报会上，站出来讲成果的是另一个同事。",
      "领导记住了那个同事思路清楚、推进有力，却不知道小陈在背后做了多少。小陈很不舒服，却又觉得主动说功劳显得不体面。",
      "问题在于，组织不是全知系统。很多贡献如果没有被结构化呈现，就会在权力视野里消失。"
    ],
    analysis: [
      {
        title: "贡献需要进入决策者视野",
        body: "不是所有努力都会自然被看见。领导通常通过汇报、结果、风险承担来识别贡献。只做不说，等于把解释权交给别人。"
      },
      {
        title: "会表达不是邀功，而是建立归属",
        body: "成熟的表达不是抢功，而是说清楚：我承担了什么、解决了什么、带来了什么结果。功劳归属不清，下一次资源分配就会失真。"
      },
      {
        title: "隐形劳动最容易被低估",
        body: "越是基础、细碎、补洞型工作，越需要被记录。否则它只在出问题时被看见，不出问题时反而像没有发生。"
      }
    ],
    insights: [
      { label: "对下", text: "努力要留下痕迹，结果要有表达，否则别人很难替你争取资源。" },
      { label: "对上", text: "领导需要可判断的信息，不是猜你的辛苦。" },
      { label: "社交人情", text: "不争不等于清高，有时只是把自己的利益让给更会表达的人。" }
    ],
    questionText: "我最近做过哪件重要但不容易被看见的事？它有没有留下可被理解的成果？",
    actionText: "花 10 分钟写一条三句话汇报：我做了什么、解决了什么、对结果有什么帮助。"
  }
];

const storageKey = "daily-cognitive-web-state";
const todayKey = new Date().toISOString().slice(0, 10);
const els = {
  streakDays: document.querySelector("#streakDays"),
  todayDate: document.querySelector("#todayDate"),
  cardTitle: document.querySelector("#cardTitle"),
  themeStrip: document.querySelector("#themeStrip"),
  caseText: document.querySelector("#caseText"),
  analysisList: document.querySelector("#analysisList"),
  insightList: document.querySelector("#insightList"),
  questionText: document.querySelector("#questionText"),
  actionText: document.querySelector("#actionText"),
  reflectionInput: document.querySelector("#reflectionInput"),
  saveReflection: document.querySelector("#saveReflection"),
  completeAction: document.querySelector("#completeAction"),
  prevCard: document.querySelector("#prevCard"),
  nextCard: document.querySelector("#nextCard"),
  doneCount: document.querySelector("#doneCount"),
  reviewGrid: document.querySelector("#reviewGrid"),
  historyList: document.querySelector("#historyList"),
  clearHistory: document.querySelector("#clearHistory"),
  toast: document.querySelector("#toast")
};

let state = loadState();
let cardIndex = 0;

function loadState() {
  try {
    return JSON.parse(localStorage.getItem(storageKey)) || { records: [] };
  } catch {
    return { records: [] };
  }
}

function saveState() {
  localStorage.setItem(storageKey, JSON.stringify(state));
}

function currentCard() {
  return cards[cardIndex % cards.length];
}

function renderToday() {
  const card = currentCard();
  const record = state.records.find((item) => item.date === todayKey && item.title === card.title);
  els.todayDate.textContent = new Intl.DateTimeFormat("zh-CN", {
    month: "long",
    day: "numeric",
    weekday: "long"
  }).format(new Date());
  els.cardTitle.textContent = card.title;
  els.themeStrip.innerHTML = [card.theme, ...card.tags]
    .map((tag) => `<span class="theme-chip">${escapeHtml(tag)}</span>`)
    .join("");
  els.caseText.innerHTML = card.caseText.map((text) => `<p>${escapeHtml(text)}</p>`).join("");
  els.analysisList.innerHTML = card.analysis.map((item, index) => `
    <article class="analysis-item">
      <span>${index + 1}</span>
      <div>
        <h3>${escapeHtml(item.title)}</h3>
        <p>${escapeHtml(item.body)}</p>
      </div>
    </article>
  `).join("");
  els.insightList.innerHTML = card.insights.map((item) => `
    <article class="insight-item">
      <strong>${escapeHtml(item.label)}</strong>
      <p>${escapeHtml(item.text)}</p>
    </article>
  `).join("");
  els.questionText.textContent = card.questionText;
  els.actionText.textContent = card.actionText;
  els.reflectionInput.value = record?.reflection || "";
  renderSummary();
}

function renderSummary() {
  const records = [...state.records].sort((a, b) => b.date.localeCompare(a.date));
  const completed = records.filter((item) => item.completed);
  els.streakDays.textContent = calculateStreak(records);
  els.doneCount.textContent = `${completed.length} 次`;
  els.reviewGrid.innerHTML = buildReviewItems(records);
  els.historyList.innerHTML = records.length ? records.map(buildHistoryItem).join("") : `<div class="empty">还没有记录。今天保存一次，就会在这里出现。</div>`;
}

function calculateStreak(records) {
  const days = new Set(records.filter((item) => item.completed || item.reflection).map((item) => item.date));
  let count = 0;
  const cursor = new Date(todayKey);
  while (days.has(cursor.toISOString().slice(0, 10))) {
    count += 1;
    cursor.setDate(cursor.getDate() - 1);
  }
  return count;
}

function buildReviewItems(records) {
  const latest = records.slice(0, 4);
  if (!latest.length) {
    return `<div class="empty">先完成几张卡片，复盘会自动长出来。</div>`;
  }
  return latest.map((item) => `
    <div class="review-item">
      <span>${escapeHtml(item.date)} · ${escapeHtml(item.theme)}</span>
      <strong>${escapeHtml(item.title)}</strong>
      <p>${escapeHtml(item.reflection || "还没有写回答。")}</p>
    </div>
  `).join("");
}

function buildHistoryItem(item) {
  return `
    <div class="history-item">
      <span>${escapeHtml(item.date)} · ${item.completed ? "已完成行动" : "未标记行动"}</span>
      <strong>${escapeHtml(item.title)}</strong>
      <p>${escapeHtml(item.reflection || "没有回答内容。")}</p>
    </div>
  `;
}

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

function upsertToday(patch) {
  const card = currentCard();
  const existing = state.records.findIndex((item) => item.date === todayKey && item.title === card.title);
  const nextRecord = {
    date: todayKey,
    title: card.title,
    theme: card.theme,
    reflection: "",
    completed: false,
    ...patch
  };
  if (existing >= 0) {
    state.records[existing] = { ...state.records[existing], ...nextRecord };
  } else {
    state.records.push(nextRecord);
  }
  saveState();
  renderSummary();
}

function switchView(viewName) {
  document.querySelectorAll(".tab").forEach((tab) => {
    tab.classList.toggle("is-active", tab.dataset.view === viewName);
  });
  document.querySelectorAll(".view").forEach((view) => {
    view.classList.toggle("is-active", view.id === `${viewName}View`);
  });
}

function showToast(text) {
  els.toast.textContent = text;
  els.toast.classList.add("is-visible");
  window.setTimeout(() => els.toast.classList.remove("is-visible"), 1800);
}

document.querySelectorAll(".tab").forEach((tab) => {
  tab.addEventListener("click", () => switchView(tab.dataset.view));
});

els.saveReflection.addEventListener("click", () => {
  upsertToday({ reflection: els.reflectionInput.value.trim() });
  showToast("今日记录已保存");
});

els.completeAction.addEventListener("click", () => {
  upsertToday({ reflection: els.reflectionInput.value.trim(), completed: true });
  showToast("微行动已标记完成");
});

els.prevCard.addEventListener("click", () => {
  cardIndex = (cardIndex - 1 + cards.length) % cards.length;
  renderToday();
});

els.nextCard.addEventListener("click", () => {
  cardIndex = (cardIndex + 1) % cards.length;
  renderToday();
});

els.clearHistory.addEventListener("click", () => {
  state = { records: [] };
  saveState();
  renderToday();
  showToast("历史记录已清空");
});

renderToday();
